namespace PersonalTaskManager.Core.Enums
{
    /// <summary>Trạng thái Kanban — map với cột STATUS (1–3).</summary>
    public enum TaskStatus
    {
        ToDo = 1,
        InProgress = 2,
        Done = 3
    }
}
