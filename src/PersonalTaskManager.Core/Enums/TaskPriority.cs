namespace PersonalTaskManager.Core.Enums
{
    /// <summary>Mức độ ưu tiên task — map với cột PRIORITY (1–3).</summary>
    public enum TaskPriority
    {
        Low = 1,
        Medium = 2,
        High = 3
    }
}
