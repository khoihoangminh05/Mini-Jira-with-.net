@{
    ViewBag.Title = "Trang chủ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="hero text-center py-5">
    <h1 class="display-5 fw-bold">Xin chào, @ViewBag.Username!</h1>
    <p class="lead text-muted">Personal Task Manager — @ViewBag.Phase</p>
    <hr class="my-4" />
    <p>Bạn đã đăng nhập thành công. MVP mentor đã sẵn sàng — quản lý dự án, task và báo cáo.</p>
    <a class="btn btn-primary btn-lg me-2" href="@Url.Action("Index", "Project")">Xem dự án</a>
    <a class="btn btn-outline-primary btn-lg me-2" href="@Url.Action("Index", "Report")">Báo cáo</a>
    <a class="btn btn-outline-secondary btn-lg" href="@Url.Action("About", "Home")">Kiểm tra DB</a>
</div>

<div class="row mt-4">
    <div class="col-md-4">
        <div class="card h-100 shadow-sm">
            <div class="card-body">
                <h5 class="card-title">Dự án</h5>
                <p class="card-text">Tạo và quản lý dự án cá nhân — đã sẵn sàng.</p>
                <a class="btn btn-sm btn-primary" href="@Url.Action("Index", "Project")">Mở dự án</a>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card h-100 shadow-sm">
            <div class="card-body">
                <h5 class="card-title">Task / Kanban</h5>
                <p class="card-text">Kéo thả task giữa các cột — SortableJS Phase 8.</p>
                <a class="btn btn-sm btn-outline-primary" href="@Url.Action("Index", "Project")">Mở Kanban</a>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card h-100 shadow-sm">
            <div class="card-body">
                <h5 class="card-title">Báo cáo</h5>
                <p class="card-text">Tiến độ hoàn thành theo dự án — progress bar.</p>
                <a class="btn btn-sm btn-primary" href="@Url.Action("Index", "Report")">Xem báo cáo</a>
            </div>
        </div>
    </div>
</div>
